import styled from 'styled-components';

export const ContainerMyAccount = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 8px;
`;

export const TitleMyAccount = styled.p`
  color: var(--grey-color);
  text-align: center;
  font-family: Roboto;
  font-size: 15px;
  font-style: normal;
  font-weight: 400;
  line-height: normal;
`;
